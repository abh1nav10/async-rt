[package]
name = "async-runtime"
version = "0.1.0"
edition = "2024"
authors = ["abh1nav10"]

[dependencies]
flume = "0.12"
mio = { version = "1.1.1", features = ["os-poll", "net"] }
rand = "0.10"
futures-util = {version = "0.3.32", features = ["io"]}
